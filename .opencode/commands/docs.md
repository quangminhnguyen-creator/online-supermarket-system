---
description: Update documentation for an approved task
agent: docs
subtask: true
---

Process documentation for the compact task ID and mode identified by:

`$ARGUMENTS`

Allowed modes are `DOCS_ONLY_IMPLEMENTATION`, `DOCS_REVIEW_FIX`, and `POST_APPROVAL_SYNC`. Follow the mode prerequisites in the Docs agent contract and write `.ai/results/TASK-NNN-DOCS.md` with exact evidence for every mode.

Do not expect copied task text, source content, diffs, history, or prior results in this prompt. Read only the bounded paths named by the task contract.
